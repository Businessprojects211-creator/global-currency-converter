"use client";

import { useEffect, useState } from "react";
export default function Settings() { const [theme, setTheme] = useState("system"); useEffect(() => setTheme(localStorage.getItem("global-convert-theme") || "system"), []); function update(value: string) { setTheme(value); localStorage.setItem("global-convert-theme", value); document.documentElement.dataset.theme = value; } return <><header className="site-header"><a className="brand" href="/"><span className="brand-mark">↗</span> Global<span>Convert</span></a><a className="workspace-back" href="/dashboard">← Workspace</a></header><main className="settings-page"><div className="settings-heading"><p className="eyebrow">PREFERENCES</p><h1>Settings.</h1><p>Shape the way GlobalConvert feels on this device. Your preference is stored locally in your browser.</p></div><section className="settings-card" aria-labelledby="appearance-title"><div className="settings-card-heading"><div><span className="settings-kicker">DISPLAY</span><h2 id="appearance-title">Appearance</h2></div><span className="settings-status">{theme === "system" ? "Automatic" : theme === "dark" ? "Dark mode" : "Light mode"}</span></div><p className="settings-description">Choose a color theme for the converter and workspace.</p><label className="settings-field" htmlFor="theme"><span>Color theme</span><select id="theme" value={theme} onChange={(event) => update(event.target.value)}><option value="system">Use system preference</option><option value="light">Light</option><option value="dark">Dark</option></select></label><p className="settings-note">System preference follows your device settings. Changes apply immediately and stay on this device.</p></section><a className="settings-return" href="/">Return to converter <span>→</span></a></main></>; }
