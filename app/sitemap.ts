import type { MetadataRoute } from "next";
import { currencies } from "@/lib/currencies";
import { guides } from "@/lib/guides";
import { getSiteUrl } from "@/lib/site-url";
export default function sitemap(): MetadataRoute.Sitemap { const base = getSiteUrl(); const staticPages = ["", "/calculator", "/guides", "/popular-pairs", "/faq", "/about", "/contact", "/rate-methodology", "/privacy", "/cookies", "/terms", "/disclaimer"]; const popularPairs = [["USD", "EUR"], ["USD", "GBP"], ["USD", "UGX"], ["UGX", "USD"], ["USD", "KES"], ["USD", "TZS"], ["USD", "NGN"], ["EUR", "USD"], ["GBP", "USD"], ["EUR", "UGX"], ["GBP", "UGX"]]; const pairUrls = popularPairs.map(([from, to]) => ({ url: `${base}/convert/${from.toLowerCase()}/${to.toLowerCase()}`, lastModified: new Date() })); const guideUrls = guides.map((guide) => ({ url: `${base}/guides/${guide.slug}`, lastModified: new Date() })); return [...staticPages.map((path) => ({ url: `${base}${path}`, lastModified: new Date() })), ...guideUrls, ...pairUrls]; }
