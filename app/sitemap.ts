import type { MetadataRoute } from "next";
import { currencies } from "@/lib/currencies";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"; const staticPages = ["", "/about", "/contact", "/rate-methodology", "/calculator", "/disclaimer", "/privacy-policy", "/terms", "/cookie-policy", "/history", "/multi-currency-converter", "/search"]; const popularPairs = [["USD", "EUR"], ["USD", "GBP"], ["USD", "UGX"], ["USD", "KES"], ["EUR", "USD"], ["GBP", "USD"]]; return [...staticPages.map((path) => ({ url: `${base}${path}`, lastModified: new Date() })), ...currencies.map((currency) => ({ url: `${base}/currency/${currency.code.toLowerCase()}`, lastModified: new Date() })), ...popularPairs.map(([from, to]) => ({ url: `${base}/convert/${from}/${to}`, lastModified: new Date() }))]; }
